import { LogRequest, LogResponse } from '../../formatters';
import { WorldState } from '../world/world-state';

export function getLogs(worldState: WorldState, logRequest: LogRequest): LogResponse[] {
  return [];
}
